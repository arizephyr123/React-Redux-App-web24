export const initialState = {
    weatherData: {},
    isFetching: false,
    error: null
};

function reducer(state = initialState, action){
    switch(action.type){
    default:
        return state}
};

export default reducer;